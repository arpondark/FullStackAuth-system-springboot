spring.application.name=FullStackAuth

spring.datasource.url=jdbc:mysql://localhost:3306/springdb
spring.datasource.username=root
spring.datasource.password=arpon007
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# JWT secret used to sign tokens (HS256). Must be at least 32 bytes for HMAC-SHA256.
# In production, DO NOT commit the secret. Load from environment or a secret manager.
# Example env var override: set JWT_SECRET_KEY=... and reference it with ${JWT_SECRET_KEY}
jwt.secret.key=sfgskfhshfijideferfderastgwyaeasgdgdgaAbBcCdDeEfFgGhHiIjJkKlLmMnNoOpP

# Application base URL for email links
app.url=${FRONTEND_URL}

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
# Values are provided via .env and spring-dotenv
spring.mail.username=${EMAIL_USER}
spring.mail.password=${EMAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Global API Prefix - all endpoints will be prefixed with /api/v1.1
# This allows you to version your API and change it in one place
server.servlet.context-path=/api/v1
adminEmail=${adminEmail}
adminPass=${adminPass}

# Google OAuth2 Configuration
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=email,profile
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.google.client-name=Google
